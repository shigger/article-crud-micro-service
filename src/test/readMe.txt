For Integration Testing

Import the postman exported json into Postman to set up and use RESTful API tests.
Further we could integrate these with our CI tool so they run automatically.
Integration has been successful but the tests are failing currently as some configuration is missing (TODO).